# Porta padrão do servidor
PORT=5000

# Chave secreta usada para assinar os tokens JWT
JWT_SECRET=allcut_secret_key_2025

# (Opcional) Email padrão do sistema para notificações simuladas
SYSTEM_EMAIL=allcut@no-reply.com
