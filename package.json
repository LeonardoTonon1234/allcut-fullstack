{
  "name": "allcut-fullstack-root",
  "private": true,
  "scripts": {
    "dev": "concurrently -n API,WEB -c auto,auto \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm --prefix backend run dev",
    "dev:web": "npm --prefix frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
